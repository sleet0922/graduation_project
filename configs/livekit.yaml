port: 7880
bind_addresses:
  - 0.0.0.0
rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
redis:
  address: 127.0.0.1:6379
# The OpenRC livekit service writes a runtime config with the configured key/secret.
# Keeping this template credential-free prevents secrets entering images.
keys: {}
