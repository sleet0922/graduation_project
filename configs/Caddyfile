{
    admin off
    auto_https disable_redirects
}

http://:81 {
    redir https://{host}:444{uri} permanent
}

https://:444 {
    tls /etc/caddy/certs/1.pem /etc/caddy/certs/1.key
    encode zstd gzip

    header {
        -Server
        Strict-Transport-Security "max-age=31536000"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "strict-origin-when-cross-origin"
    }

    # LiveKit's SDK appends /rtc/v1 (or /rtc/v0) to the configured URL, and
    # the LiveKit server expects that complete path.
    @livekit path /rtc /rtc/*
    handle @livekit {
        reverse_proxy 127.0.0.1:7880
    }

    handle {
        reverse_proxy 127.0.0.1:8081
    }
}
